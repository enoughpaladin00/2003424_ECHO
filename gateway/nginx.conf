# Minimal configuration to prevent Nginx from crashing on startup
events {
    worker_connections 1024;
}

http {
    server {
        listen 80;
        server_name localhost;

        location / {
            return 200 'E.C.H.O. Gateway is alive. Routes pending.\n';
            add_header Content-Type text/plain;
        }
    }
}